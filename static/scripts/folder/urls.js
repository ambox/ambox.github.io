define(['scope', 'page'], function(scope, page){

	function Router(opts){
	}

	Router.prototype.request = function(path, data){
	};

	Router.prototype.render = function(path, data){
	};

	return scope.uri('app', Router);
});