define(['scope', 'page'], function(scope, page){

	function Route(opts){
	}

	Route.prototype.get = function(path, data){
	};

	Route.prototype.render = function(path, data){
	};

	return scope.uri('folder.ctrl.Route', Route);
});